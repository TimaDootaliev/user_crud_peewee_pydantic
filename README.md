# peewee + pydantic

python3 -m venv venv

. venv/bin/activate

pip install -r requirements.txt

createdb <db_name>

create file ".env" and fill it like in ".env.template"

## to run

python3 app/main.py